# A1 Phase-0 Day-0 Operationalization Run — 2026-06-27 (Final Corrected)

**Branch:** feat/incentive-ops-phase0-operations (head post re-review)
**All 17 non-ACTIONABLE (PENDING + unverified captured).**
**No merge, no 14-day baseline.**

## Re-review blockers fixed (1-3)

1. **NaN/Inf financial bypasses**:
   - EVScenarioInputs __post_init__: isfinite on all 13 numeric fields.
   - PilotCaps: isfinite + >=0 on limits.
   - validate_caps: isfinite checks + raise CapsExceeded on NaN/Inf for usd in ledger/candidate.
   - load_ledger: isfinite on usd + others (already had for gas etc).
   - Tests: direct nan/inf bypass cases + ledger.

2. **READY provenance not enforced**:
   - Added EVReadiness enum (UNREADY/READY).
   - _has_acceptable_provenance: requires non-bad (no UNKNOWN/placeholder/"from sidecar"/"test" etc) entry for ALL 13 material inputs when READY.
   - Updated actionability promotion to check.
   - Positive test uses full good prov; bad prov test asserts BLOCKED_PROMOTION.

3. **Verif audit fields not enforced**:
   - Enhanced _is_verified: requires verified_at (non-null), official_round_terms_url, captured_source_url, raw_evidence_path.
   - Matches: urls == each other and == cap.source_url; raw_path == cap.raw_path.
   - Jurisdiction: must be ELIGIBLE (UNKNOWN/INELIGIBLE block).
   - Updated VerificationRecord to typed JurisdictionStatus enum.
   - Captured sidecars: verified_at=null , jurisdiction_status=UNKNOWN (not 'True').
   - Positive test now constructs full matching audit ver.
   - load/write updated for enums.

## Sidecars / state
- Captured (8): sha+raw_path bind, but terms=false, live=false, verified_at=null, jurisdiction=UNKNOWN.
- Pending (9): PENDING sha, false/false/null/UNKNOWN.
- EV (17): UNREADY + full 13+ prov entries (no bad keywords in READY sense).
- All non-ACTIONABLE.

## Tests / CI
- 17+ in actionability file, 38 total incentive.
- New: nan/inf, prov, audit fields.
- ruff/format/diff-check/pytest clean.
- GitHub CI would pass.

## Report note
Captured remain unverified pending human round-terms review. No capital exposure.

(Generated after final 3 blockers.)
