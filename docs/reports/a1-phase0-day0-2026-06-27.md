# A1 Phase-0 Day-0 Operationalization Run — 2026-06-27

**Branch:** feat/incentive-ops-phase0-operations (from 30ae705)
**Date:** 2026-06-27
**Operator constraints observed:** read-only paper inventory. **No wallets, no eligibility calls, no capital, no signing.** All 17 records **remain non-ACTIONABLE** (reviewer_decision **PENDING**).

## Corrections applied (per review)

- Verif sidecars now correct: 8 captured use *exact* capture sha + retained raw_path, terms=true (for captured); 9 pending use PENDING_TOOL_CAPTURE + terms=false + verified_at=None.
- Hash+id binding enforced in _is_verified (ver == cap).
- Caps use EV-sidecar .capital (no override 0); missing/<=0 blocks at caps gate.
- EV sidecars: readiness=UNREADY + per-field provenance (UNKNOWN) for all inputs; numbers are placeholders only.
- Verif sidecar supersedes registry live_round_status=UNVERIFIED.
- load_ledger: supplied missing path raises ValidationError; all numeric fields validated.
- Durable raw: removed ignore, raw *.raw now tracked in git (8 files committed with shas).
- Added 8+ new unit tests covering every gate, binding, unready, capital, ledger raise, supersede.
- Report regenerated; git diff --check clean.
- All still non-ACTIONABLE via PENDING.

## Artifacts (final)

- Code changes in types, capture, actionability, accounting, cli, __init__, tests.
- 17 verifications/*.yaml + 17 ev_inputs/*.yaml (with correct binding/UNREADY).
- 8 raw snapshots now in git for durable evidence.
- docs/reports/a1-phase0-day0-2026-06-27.md (this)
- Tests: positive path (approved+real cap+ready ev -> ACTIONABLE), negatives for all blockers.

## Day-0 run (post-correction)

```bash
python -m tools.incentive_ops validate
python -m tools.incentive_ops classify --check
python -m tools.incentive_ops actionability
# + ev sidecar loads, ledger tests, etc.
```

### Key outputs

- validate: OK (35 warnings for PENDING/UNVERIFIED as expected)
- classify --check: 17/17 exact match (5/4/3/2/3)
- actionability: all_non_actionable=True (17 BLOCKED_* via PENDING or needs-capture)
- ev: readiness=UNREADY, base_ev negative (0 contrib from spec)
- ledger: empty ok (no supplied missing in run)
- binding verified in python checks: captured shas match exactly

## Invariants

- No ACTIONABLE possible (PENDING reviewer gate).
- Future promotion requires: real captured sha in verif==cap, terms+live=true, APPROVED, READY ev, positive capital from sidecar, caps pass, tier ok.
- Raw bytes + sha committed and bound.
- All changes ruff clean, new+old tests pass.

Do not start measurement window; this is corrected Day-0 baseline only after human review.
