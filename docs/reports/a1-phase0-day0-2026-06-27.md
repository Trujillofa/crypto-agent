# A1 Phase-0 Day-0 Operationalization Run — 2026-06-27 (Corrected)

**Branch:** feat/incentive-ops-phase0-operations
**Commit:** (post re-review fixes)
**Date:** 2026-06-27
**Key:** All 17 records remain **non-ACTIONABLE**. Captured records have **terms_match_snapshot=false**, **live_round_open=false** until explicit human review of round-specific official terms (not homepages). No 14-day baseline started.

## Fixes for re-review blockers

1. CI/lint: ruff format applied to test file.
2. All 8 raw snapshots now tracked in git.
3. .gitattributes marks snapshots/**/*.raw as binary (git diff --check clean for them).
4. All captured verifs: terms/live explicitly false (no fabricated "verified").
5. VerificationRecord extended with required audit fields (official_round_terms_url, captured_source_url, jurisdiction_status, eligibility_*/claim/vesting dates).
6. All 17 EV sidecars now have provenance entry for *every* material input field (still UNREADY; READY will require them).
7. Positive test now uses tmp_path + real written bytes + actual computed SHA; full ensure_raw_evidence + bind path exercised (no mock).
8. load_ledger now rejects NaN/Inf and negative gas_usd/hours (realized_usd allows signed).

## Schema (VerificationRecord additions for #5)

- official_round_terms_url
- captured_source_url
- jurisdiction_status
- eligibility_open/close, claim_date, vesting_end

Sidecars populated (nulls for day0; captured_source_url from registry).

## Artifacts

- Updated verif + ev sidecars (17 each)
- All 8 .raw tracked + binary attr
- .gitattributes
- Extended types + load/write
- Updated + new tests (12 in file, all pass)
- This report (captured remain unverified)

## Day-0 commands run (no wallets/eligibility/capital)

validate, classify --check (17/17 match), actionability (all non-ACTIONABLE), ev (via sidecars), report, deadlines.

### Sample actionability
... (mix BLOCKED_UNVERIFIED for captured+pending; NEEDS_CAPTURE for uncaptured)
All non-ACTIONABLE: True

## Current sidecar state (post #4)

- 8 "captured": sha matches cap, raw_path correct, but **terms=false, live=false**
- 9 pending: PENDING sha, terms=false, live=false, verified_at=null

UNREADY EV + full provenance.

Do not merge. Regenerated report. Full CI re-run below.

(Generated after all 8 re-review fixes)
