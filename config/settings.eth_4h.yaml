agent_id: eth-4h
display_name: eth-4h-simple-ma

trading:
  pairs:
    - ETHUSDT
  timeframe: 4h

trading_execution:
  enabled: false

futures:
  symbols:
    - ETHUSDT

strategy:
  strategies:
    - name: simple_ma
      config: {}
  aggregator:
    btc_regime_filter_enabled: true
    btc_reference_symbol: BTCUSDT
    btc_dump_threshold_pct: -1.0
    btc_dump_require_below_ema200: true
