global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "trading-agent"
    file_sd_configs:
      - files:
          - /etc/prometheus/file_sd/agents.json
