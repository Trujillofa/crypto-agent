agent_id: avax-4h-ma
display_name: avax-4h-simple-ma

# Reverted to paper: live Binance spot account has $0 USDT balance (2026-04-24).
# CCI breakout strategy also 0/6 win rate — not ready for live capital.
# Re-enable live mode once account is funded and edge is validated.
mode: paper

trading:
  pairs:
    - AVAXUSDT
  timeframe: 4h

trading_execution:
  test_mode: true
  use_atr_sizing: false
  order_size_usdt: 7.0

futures:
  enabled: false

strategy:
  default_trading_mode: spot
  strategies:
    - name: cci_breakout
      config: {}
  aggregator:
    btc_regime_filter_enabled: false

# Temporary: Use REST for backfill
ingest:
  use_websocket: false
