# =====================================================================
# VERIFICATION SIDECAR — coinlist-token-sale   (IN_CORE)
# Gate: tools/incentive_ops/actionability.py::_is_verified
# Human attestation. Each field must satisfy the rule in its comment for the
# candidate to clear Gate 2 (Verified). reviewer_decision=APPROVED is YOUR
# personal sign-off that you read the live official terms — do not approve
# anything you have not actually read.
# =====================================================================
id: coinlist-token-sale

# --- cryptographic binding to capture (DO NOT EDIT — must match captures/coinlist-token-sale.yaml) ---
snapshot_sha256: 8b8996df87a1a8456ad3f3e006d4c5bc77507eb0ce190f078c7a75dae668972c   # == capture.snapshot_sha256 (refreshed 2026-06-30)
raw_evidence_path: research/a1-incentive-farming/snapshots/coinlist-token-sale/20260630T172539Z_8b8996df87a1a845.raw  # == capture.raw_path
captured_source_url: https://coinlist.co/token-launches   # == capture.source_url

# --- human attestation (FILL after reading the LIVE official round terms) ---
# NOTE: the captured page is the CoinList *listings* page, not a single sale's
# terms. The gate requires official_round_terms_url == captured_source_url, so to
# verify a SPECIFIC sale you must RE-CAPTURE that sale's own terms page first
# (run the capture tool against the sale URL); this sidecar then binds to it.
verified_at: null                 # FILL: ISO-8601 UTC, e.g. 2026-06-30T14:00:00+00:00
official_round_terms_url: null    # FILL: must == captured_source_url; the official terms page you actually read
terms_match_snapshot: false       # FILL: true ONLY if live terms still match the captured snapshot bytes
live_round_open: false            # FILL: true ONLY if the sale/round is open right now
jurisdiction_status: UNKNOWN      # FILL: ELIGIBLE | INELIGIBLE | UNKNOWN — must be ELIGIBLE (your country allowed + KYC passable)
reviewer_decision: PENDING        # FILL: PENDING -> APPROVED is the final human gate

# --- schedule metadata (optional; feeds the deadline monitor) ---
eligibility_open: null            # FILL: ISO date window opens
eligibility_close: null           # FILL: ISO date window closes
claim_date: null                  # FILL: ISO date reward claimable
vesting_end: null                 # FILL: ISO date vesting ends
notes: ""                         # FILL: per-identity cap, sybil/KYC rules, anything unusual
