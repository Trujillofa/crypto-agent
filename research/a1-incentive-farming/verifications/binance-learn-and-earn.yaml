# =====================================================================
# VERIFICATION SIDECAR — binance-learn-and-earn   (IN_CORE)
# Gate: tools/incentive_ops/actionability.py::_is_verified
#
# !!! CAPTURED, BUT AGAINST THE WRONG PAGE — DO NOT ATTEST terms_match_snapshot=true !!!
# Gate 1 is now mechanically satisfied (real sha + raw bytes), BUT the registry's
# official_source_url for this id is `academy.binance.com/ur-PK/bitcoin` — a generic
# Bitcoin explainer article, NOT the Learn-&-Earn rewards terms. This evidence is
# useless for verifying a Learn-&-Earn reward. To fix properly a HUMAN must:
#   1. add `www.binance.com` (path `/en/learn-and-earn`) to
#      config/incentive_ops/endpoint_allowlist.yaml, and
#   2. point this id's official_source_url in starter-registry-v0.yaml at the real
#      Learn-&-Earn page, then re-capture. Until then leave terms_match_snapshot=false.
# =====================================================================
id: binance-learn-and-earn

# --- cryptographic binding to capture (synced to captures/binance-learn-and-earn.yaml — WRONG PAGE, see header) ---
snapshot_sha256: 8f4cc7da546c42e70bb4147036e610994872cdae6cfbd91d44c501528021e86d   # == capture.snapshot_sha256 (academy article, not Learn-&-Earn)
raw_evidence_path: research/a1-incentive-farming/snapshots/binance-learn-and-earn/20260630T172526Z_8f4cc7da546c42e7.raw  # == capture.raw_path
captured_source_url: https://academy.binance.com/ur-PK/bitcoin   # == capture.source_url (registry points here — wrong page)

# --- human attestation (FILL after capture + reading the LIVE official terms) ---
# Learn-&-Earn = fixed token reward for completing a course/quiz; typically zero
# capital, labor-driven. Confirm the specific course reward and quota are live.
verified_at: null                 # FILL: ISO-8601 UTC, e.g. 2026-06-30T14:00:00+00:00
official_round_terms_url: null    # FILL: must == captured_source_url; the official course-terms page you read
terms_match_snapshot: false       # FILL: true ONLY if live terms still match the captured snapshot bytes
live_round_open: false            # FILL: true ONLY if the course/reward quota is open right now
jurisdiction_status: UNKNOWN      # FILL: ELIGIBLE | INELIGIBLE | UNKNOWN — must be ELIGIBLE (Binance available + KYC in your country)
reviewer_decision: PENDING        # FILL: PENDING -> APPROVED is the final human gate

# --- schedule metadata (optional; feeds the deadline monitor) ---
eligibility_open: null            # FILL: ISO date course opens
eligibility_close: null           # FILL: ISO date course/quota closes
claim_date: null                  # FILL: ISO date reward distributed
vesting_end: null                 # FILL: usually none for Learn-&-Earn (locked-product variants vest)
notes: ""                         # FILL: course id, reward token + qty, quota/first-come limits
